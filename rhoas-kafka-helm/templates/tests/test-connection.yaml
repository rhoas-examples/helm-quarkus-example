apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rhoas-kafka-helm.fullname" . }}-test-connection"
  labels:
    {{- include "rhoas-kafka-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rhoas-kafka-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
