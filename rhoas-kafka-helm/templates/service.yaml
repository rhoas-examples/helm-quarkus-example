apiVersion: v1
kind: Service
metadata:
  name: {{ include "rhoas-kafka-helm.fullname" . }}
  labels:
    {{- include "rhoas-kafka-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rhoas-kafka-helm.selectorLabels" . | nindent 4 }}
